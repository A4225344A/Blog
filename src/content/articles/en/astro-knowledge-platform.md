---
id: astro-knowledge-platform-en
slug: astro-knowledge-platform
title: "Organizing bilingual articles, series and search with Astro"
description: "Articles in two languages: a look at the files that connect URLs, series order, Content Collections, Pagefind and deployment checks."
locale: en
translationKey: astro-knowledge-platform
contentType: tutorial
difficulty: intermediate
topics: [web-foundations, platform-engineering]
skills: [astro-content-modeling, static-site-delivery]
prerequisiteSkills: []
recommendedArticles: []
publishedAt: 2026-09-18
updatedAt: 2026-09-24
status: published
---

Each article has a Chinese and an English version and appears in both a series and a topic list. I do not want to hunt through those lists whenever a title or URL changes. I store the relationships as data and let the build generate the links.

This article follows the files behind that setup. To build from an empty project, follow the earlier tutorials in the series first.

The code uses Astro 7.3.3 and TypeScript, with Markdown for articles and Pagefind for search. You can find the paths below in the [GitHub repository](https://github.com/A4225344A/Blog).

## Run the repository locally

Use the series setup: Node.js 24.x and pnpm 10.32.1. Install Node.js and Git as described in article two, then run `npm.cmd install --global pnpm@10.32.1` in PowerShell. The repository accepts Node 22.12 or later; these instructions consistently use 24.x.

```powershell
git clone https://github.com/A4225344A/Blog.git
cd Blog
pnpm.cmd install --frozen-lockfile
pnpm.cmd run content:validate
pnpm.cmd run test
pnpm.cmd run check
pnpm.cmd run build
pnpm.cmd run test:build
pnpm.cmd run preview
```

`--frozen-lockfile` uses the committed dependency resolution. `check` runs Astro and TypeScript diagnostics. `build` generates `dist` and the Pagefind index, then `test:build` inspects links and metadata in the resulting files.

Use `pnpm.cmd run dev` while editing. Search needs an index generated by build, so stop dev, build and preview when testing search. Preview serves the previous build; changing a source file does not update that output until another build.

## One article, several ways to find it

```text
src/content/
  articles/zh-tw/     Traditional Chinese articles
  articles/en/        English articles
  topics/            Browsing categories
  skills/            Capabilities and prerequisites
  learning-paths/    Ordered article series
  projects/          Project information
```

Articles use Markdown with frontmatter: metadata between the opening `---` lines. The other four entities use JSON. Their schemas, or accepted fields and types, live in `src/content/schemas.ts`.

For example, “Why I use Astro” appears under Website Engineering and in the Astro series. Both links open `/Blog/en/blog/why-astro/`. Editing the body means changing just one Markdown file.

### Identity, translations and URLs

| Field | Purpose | Effect of a change |
| --- | --- | --- |
| `id` | Stable content identity | Series and projects reference it; keep it when changing titles |
| `translationKey` | Groups equivalent language versions | Language switching uses it to find the matching article |
| `slug` | Last part of the public URL | Can change with a migration notice retained at the old URL |

The first three articles changed URLs. Their old `beginner-*` pages explain the rewrite and link to the new articles. IDs stayed the same: `beginner-tools-en`, for example, now has the slug `why-astro`. The series still references its original ID.

### Who owns reading order

The series order lives in `sections[].articleIds` in `src/content/learning-paths/knowledge-platform.json`. The build filters by language and publication status without reordering the list. Both languages go from choosing Astro to creating the project, publishing an article and finally this walkthrough.

Project files list their related articles. Each Article records its topics, skills and recommended articles. To find which series an article belongs to, `src/utils/graph.ts` looks through the series lists; the article does not need a second copy of that information.

## Check source data before rendering

Astro loads content by ID. Duplicate IDs can overwrite one another before a later check sees them. `src/utils/content-source.ts` reads the original files and retains source paths before validating them.

Duplicate IDs, missing references, invalid metadata and forbidden article fields stop the build with a nonzero exit. For example, Article cannot declare `order` because the series already owns it. Diagnostics identify the source so the error can be corrected before deployment.

Validation also prints `W_ARTICLE_NO_PROJECT`. None of the five Astro articles belongs to a Project, so their two language versions produce ten warnings without stopping the build. Checks for skill dependency cycles have not been added.

## Language routes and search metadata

Traditional Chinese uses `/zh-tw/` and English uses `/en/`. General articles live under `/blog/`; troubleshooting and case studies use `/cases/`. Only `published` content enters public article lists, RSS and search.

Language switching uses `translationKey` to find the corresponding published article, then uses that version’s own URL. Without a translation, it returns to the target-language home.

`canonical` identifies the article’s primary URL, and `hreflang` lists its translations. `x-default` points to the language chooser when no language is specified. The layout generates these tags, so I do not fill them in for each article.

For example, this article’s Traditional Chinese `<head>` contains these tags. The English version points its canonical to the English URL and carries the same hreflang list. Articles do not use `x-default`; that belongs to the language chooser and home pages.

```html
<link rel="canonical" href="https://a4225344a.github.io/Blog/zh-tw/blog/astro-knowledge-platform/">
<link rel="alternate" hreflang="zh-TW" href="https://a4225344a.github.io/Blog/zh-tw/blog/astro-knowledge-platform/">
<link rel="alternate" hreflang="en" href="https://a4225344a.github.io/Blog/en/blog/astro-knowledge-platform/">
```

Shared topics and projects have one entity each. Their display translations live in `src/i18n/content.ts` and do not create extra graph entities.

## Search and reading without a backend

Pagefind reads generated article, author profile and detail-page HTML to build a local index. It excludes home pages and navigation so menus do not dominate results. The search page loads files from the deployment directory without a remote search service.

Traditional Chinese queries work, but Pagefind does not stem `zh-tw` words: it does not automatically match different word forms.

Article bodies and tables of contents are static HTML: text and section links work with JavaScript disabled. A small script remembers the light, dark or system theme. Keyboard navigation starts with a “Skip to content” link.

## GitHub Pages deployment paths

`https://name.github.io` is the origin, or scheme and host. `/Blog/` is the base deployment path. Images, article links and search results must all use that base.

These variables affect only the current PowerShell session:

```powershell
$env:SITE_URL = 'https://YOUR_USERNAME.github.io'
$env:SITE_BASE = '/Blog/'
pnpm.cmd run build
pnpm.cmd run test:build
pnpm.cmd run preview
```

Stop preview, then clear them before normal local development:

```powershell
Remove-Item Env:SITE_URL, Env:SITE_BASE -ErrorAction SilentlyContinue
pnpm.cmd run dev
```

`sitemap.xml` lists public URLs and RSS exposes article updates per language. `/Blog/robots.txt` cannot set origin-wide crawler policy; that belongs in the owner site’s `https://name.github.io/robots.txt`.

## Publish the output that was checked

The repository workflow runs installation, content validation, tests and builds on PRs and main pushes. PRs do not deploy. A successful main run preserves its output. The deployment job waits for `github-pages` approval only if Required reviewers are configured, then verifies that `main` still points to the checked commit. A SHA identifies that Git revision.

Deployment does not rebuild because I want the published files to be the files already inspected. Only that job receives Pages write and short-lived identity permissions.

To try changing the series order, edit `articleIds` in `knowledge-platform.json` and build again. The series list and the previous/next links update together, without moving any article files.
