---
import type { Locale } from '../i18n';
import { siteConfig } from '../config/site';
import { withBase } from '../utils/routes';
interface Props { locale: Locale; compact?: boolean }
const { locale, compact = false } = Astro.props;
const author = siteConfig.author;
---
<div class:list={['author', { compact }]}>
  <a class="avatar-link" href={author.githubUrl} aria-label={locale === 'zh-TW' ? `${author.name} 的 GitHub` : `${author.name} on GitHub`}>
    <img src={withBase(author.avatar, import.meta.env.BASE_URL)} alt={author.name} width="96" height="96" />
  </a>
  <div><a class="author-name" href={author.githubUrl}>{author.name}</a><p>{author.tagline[locale]}</p></div>
</div>
