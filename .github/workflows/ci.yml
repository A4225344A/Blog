name: CI
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PLAYWRIGHT_BROWSERS_PATH: .playwright
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4 (peeled commit)
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run content:validate
      - run: pnpm run test
      - run: pnpm run check
      - run: pnpm run test:collections
      - run: pnpm exec tsx scripts/configure-pages.ts
      - run: pnpm exec playwright install --with-deps chromium
      - name: Build and verify user-site paths
        env:
          SITE_BASE: /
        run: |
          pnpm run build
          pnpm run test:build
          pnpm run test:browser
      - name: Build and verify production paths
        run: |
          pnpm run build
          pnpm run test:build
          pnpm run test:browser
      - name: Preserve the exact validated site
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: verified-site
          path: dist/
          if-no-files-found: error
          retention-days: 7
